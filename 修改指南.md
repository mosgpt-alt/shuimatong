# 修改指南

仓库：<https://github.com/mosgpt-alt/shuimatong>
公开网址：<https://mosgpt-alt.github.io/shuimatong/>
本文只讲**怎么改**和**改完怎么上线**。

---

## 一、先认清哪些是「源」、哪些是「生成物」

**只改「源」。改生成物等于白改 —— 下次生成会被覆盖。**

| 想改什么 | 改这个文件（源） | 改完怎么生效 |
|---|---|---|
| 税则条目、章节、危化品、手册、归类先例 | `data\app_data.json` | 重跑 `build\bundle.py` |
| 民间叫法 / 俗称搜索 | `data\alias_folk.json` | 同上 |
| 海关总署归类决定 | `data\cls_decisions.json` | 同上 |
| 申报要素（字段名） | `data\decl_real.json` | 同上 |
| 申报要素真实示例值 | `data\decl_exam.json` | 同上 |
| 海关预归类真实实例 | `data\decl_decisions.json` | 同上 |
| 监管条件 / 检验检疫 / 随附单证 | `data\decl_reg.json` | 同上 |
| 界面、样式、交互逻辑 | `build\template.html` | 同上 |
| 后端账号 / 同步接口 | `server\*.js` | 重启服务 |
| 不想碰 JSON | `本地数据\*.csv` | 见第三节 |

**生成物（别手改，会被覆盖）**

| 文件 | 由谁生成 | 说明 |
|---|---|---|
| `site\index.html` | `build\bundle.py` | 网页本体（界面 + 内联脚本） |
| `site\data.js` | `build\bundle.py` | 全部数据，约 18 MB |
| `税码通.html` | `build\bundle.py` | 单文件离线版（双击即用，未入库） |

> 网页内容 = `template.html` + `data\*.json` 一起决定的。所以**改了数据，就必须重跑一次 `bundle.py`**，否则网页里还是旧数据。

> **构建必须带 `PYTHONHASHSEED=0`。** 不设的话 Python 的字符串哈希每个进程随机，
> `site\data.js` 和 `税码通.html` 每次生成的 key 顺序都不同（大小一样、哈希不同），
> 于是每跑一次就多出一个「假改动」提交。仓库里的 `更新并推送.bat`、`build\发布.bat`、
> `本地数据\local_data.py` 都已经帮你设好了；如果你自己手敲 `python build\bundle.py`，
> 请写成 `set PYTHONHASHSEED=0 && python build\bundle.py`。

---

## 二、改完怎么上线（一步）

双击仓库根目录的：

```
更新并推送.bat
```

它按顺序做 5 件事：

1. 跑 `build\bundle.py` 重新生成 `site\index.html` + `site\data.js`
2. `git add -A` 暂存
3. **安全闸门**：挡住账号库 / 依赖 / 固定口令，命中就中止，不推送
4. `git commit`
5. `git push` → GitHub

看到「完成。改动已上线。」就是成功了。GitHub Pages 大约 1 分钟后自动重新发布。

**只想改一个错别字 / 改界面，也走同一个脚本**，不用记别的命令。

### 如果卡在「推送」这一步（github.com 连不上）

本机实测：`github.com` 有时会被网络阻断（而 `api.github.com` 仍然通）。表现是脚本最后一步报
`Failed to connect to github.com:443 after ...`。这时换个入口，双击：

```
更新并推送（备用通道）.bat
```

它走 GitHub 官方 API（`api.github.com`）把本地已有的提交原样送上去，效果完全一样，
**不需要再输 Token**（推送凭据存在本机凭据管理器里）。看到「[OK] 推送完成」即成功。

> 两个脚本可以放心混用：备用通道会先比对「本地文件树」和「远程文件树」，
> 只补差异，并在推送后把远程提交对齐成本地的同一个提交，不会产生分叉、不会重复推。

---

## 三、不想动 JSON：用 CSV 改数据

`本地数据\` 已经备好可编辑的 CSV 和三个一键脚本：

| 脚本 | 作用 |
|---|---|
| `1 导出CSV.bat` | 把 JSON 里的数据导出成 CSV（Excel 直接开） |
| `2 回写并重建.bat` | CSV 改完 → 写回 JSON → 自动重建网站 |
| `3 体检.bat` | 检查 CSV 和 JSON 是否一致、有没有漏行 |

推荐流程：`1 导出CSV.bat` → Excel 改 → `2 回写并重建.bat` → 根目录 `更新并推送.bat`。

CSV 用 **UTF-8** 保存（Excel 的「CSV UTF-8」格式），别存成 ANSI，否则中文会乱码。

---

## 四、换一台电脑（或别人克隆下来）

```
git clone <仓库地址>
cd <仓库目录>
启动本地服务.bat        # 本机预览 http://127.0.0.1:3001
```

- 依赖会自动 `npm install`，不用手动装
- 账号数据库**不在仓库里**，首次启动自动重建，随机生成管理员密码
- 想改数据再重新出包，需要装 Python 3.x（`bundle.py` 只用标准库）

只想要网页、不要后端：直接看 `site\` 目录，两个文件就是整个网站。

---

## 五、账号库与备份

| 项 | 位置 |
|---|---|
| 数据库 | `server\data\zhiguantong.db` |
| 管理员随机密码 | `server\data\.admin_password` |
| 备份（推荐） | 双击 `备份数据库.bat` |

> **备份别只拷 `.db`。** 数据库是 WAL 模式，最新写入可能还在 `zhiguantong.db-wal` 里，
> 只拷 `.db` 会漏数据。用 `备份数据库.bat`（内部走 `VACUUM INTO`，产出单文件一致性快照），
> 或先停服务再把 `.db` / `.db-wal` / `.db-shm` 三个一起拷。

`server\data\` 已在 `.gitignore` 里，永远不会被推上去。

---

## 六、常见坑

| 现象 | 原因 / 处理 |
|---|---|
| 推不上去，`Failed to connect` | 本项目要**直连** GitHub。`git config --global --get http.proxy` 有输出就删掉 |
| 推不上去，认证失败 | 本机的 GitHub 凭据存在 GCM 里，一般不用管。真失效了就双击 `更新并推送.bat`，它会提示你粘贴一个新的 `repo` 权限 Token，并自动存回凭据管理器 |
| 推不上去，`Connection was reset` / `Failed to connect ... 21s` | **GitHub 从这台机器连是间歇性的**。等 1-2 分钟重试即可，脚本可反复跑，不会重复提交 |
| 改了数据，网页没变 | 没跑 `bundle.py`。双击 `更新并推送.bat` 即可 |
| 改了 JSON，网站崩了 | JSON 语法坏了（多逗号 / 少引号）。用第 3 节的 `3 体检.bat` 查 |
| `.bat` 双击闪一下就没了 | 根因已修：原先 bat 存成 UTF-8 且在文件里 `chcp 65001`，cmd 会按旧代码页算出的偏移继续读文件、碰到中文就「读飞」直接终止。**本仓库所有 bat 已改为 GBK 编码 + CRLF 换行 + 不切代码页**。你新增 bat 时请沿用这三条，别存成 UTF-8 |
| bat 里的中文变成乱码 | 说明文件被存成了 UTF-8。用记事本「另存为」→ 编码选 **ANSI**（即 GBK） |
| 中文在 Excel 里乱码 | CSV 要存成「CSV UTF-8」 |

---

## 七、提醒

`site\data.js` 是 18 MB 的单个文件，**每改一次数据提交一次，仓库就多存 18 MB**。
长期高频更新的话，建议给 `*.js` 上 Git LFS，或者把改动只提交到 `data\*.json`、让 `bundle.py` 现生成。
